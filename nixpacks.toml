[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install uv", "cd backend && uv sync"]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
